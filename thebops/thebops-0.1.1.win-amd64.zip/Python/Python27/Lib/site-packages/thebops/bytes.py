#!/usr/bin/python
# £££££µµµµµ -*- coding: latin1 -*- vim: sw=4 sts=4 ts=8 si et
"""
FIXME
"""

def prettify(a):
    if isinstance(a, int):
        pass
    else:
        try:
            a = int(a)
        except ValueError:
            a = int(''.join(a.strip().split('.')))
    liz = list('%d' % a)
    liz.reverse()

    aa = a
    af = float(a)
    idx = 0
    fakt = 1
    prefixes = ['k', 'M', 'G', 'T']
    while aa:
        fakt *= 1024
        aa = af / fakt
        print '%12.2f %sB' % (aa, prefixes[idx])
        if aa < 100:
            break
        idx += 1
        if idx > len(prefixes):
            break

if __name__ == '__main__':
    import sys
    prettify(sys.argv[1])
