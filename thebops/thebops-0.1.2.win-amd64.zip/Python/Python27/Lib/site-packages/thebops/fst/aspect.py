from excp import *

# @virtualclass
class Aspect(object):
    """
    Aspect of file systems or file system nodes
    """
    _aspectmap = dict({
        })
    def __init__(self, **kwargs):
        if self.__class__ is Aspect:
            raise VirtualClass('subclasses only, please')

# vim: ts=8 sts=4 si et tw=79 sw=4
